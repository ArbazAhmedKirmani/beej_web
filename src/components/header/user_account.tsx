import { Button } from "react-bootstrap";

const UserAccount = () => {
  return (
    <Button className="account-button" variant="secondary">
      Get In Touch
    </Button>
  );
};

export default UserAccount;
